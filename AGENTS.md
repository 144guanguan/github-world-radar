# GitHub 世界雷达自动化约定

本仓库由定时研究任务持续维护。自动任务必须优先保证历史可追溯、内容可复核和 Git 操作安全。

## 1. 启动与同步

1. 固定本地路径为 `/Users/admin/Documents/github-world-radar`。
2. 路径不存在时，可以从 `git@github.com:144guanguan/github-world-radar.git` 克隆到该固定路径。
3. 路径存在但不是 Git 仓库、`origin` 不匹配，或当前分支不是预期分支时，立即停止并报告；不得覆盖、重新初始化或删除现有目录。
4. 同步前检查工作区。存在非本轮任务产生的未提交修改时停止，不得清理或夹带用户改动。
5. 只允许使用 fast-forward 同步；不得 force pull、force push、改写历史或自动解决语义冲突。
6. 同一时间只允许一个更新任务运行。

## 2. 研究要求

- 使用运行时的真实 GitHub 搜索、API、Trending 页面及项目原始资料，不依赖模型记忆生成仓库。
- 每次记录绝对日期、时区、观察窗口、数据入口、数据局限和提示词版本。
- 覆盖多领域，并执行 `config/radar.yml` 中的领域配额与排除规则。
- README 中的自我描述只能标记为“项目方声明”。重要判断尽量附 Release、Commit、Issue、PR、论文、数据集说明或官方网站链接。
- 明确区分已核验事实、基于公开信号的推断和尚未验证的信息。
- 精确增量不可得时使用 `null` 和代理信号，不得编造 Star、Fork、Contributor 或增长数字。
- 入选不等于背书。医疗、金融、安全及高风险项目必须保留额外风险提示。

## 3. 内容生成

- 结构化运行数据是事实源；日报、项目档案、索引、首页和图表由生成流程统一派生。
- 同一日期、同一 edition 的重复运行必须幂等更新，不创建带 `v2`、`final` 等后缀的重复批次。
- README 只允许自动修改 `RADAR:GENERATED` 标记之间的内容。
- 每张图只表达一个主题。优先生成可追溯的数据图，如领域分布、项目状态和热度变化；不要生成没有信息价值的装饰图。
- 外部图片不得热链。只有来源、授权和归属明确时才能保存，并记录获取日期。
- 不删除历史记录。修正错误时应保留更正说明和新证据。

## 4. 验证与 Git 写入

写入前至少验证：

- 数据结构与必填字段；
- 仓库 URL 和证据 URL；
- 重复项目与稳定仓库标识；
- 领域覆盖和单领域上限；
- Markdown 内部链接；
- MkDocs 严格构建；
- 本轮 diff 仅包含预期文件。

只有存在实质变化时才提交。仅暂存本轮明确生成或修改的文件，不得使用 `git add .`。建议提交信息：

```text
radar: daily update YYYY-MM-DD
```

push 被拒绝、验证失败或发生冲突时，保留现场并报告，不得强制推送。

## 5. 安全与隐私

- Token、Cookie、SSH 私钥和其他凭据不得写入仓库、报告、提示词或日志。
- 自动推送凭据使用最小权限，仅授予目标仓库所需的 Contents 写权限。
- 不执行候选仓库中的代码，不安装其依赖，不运行未知脚本。日常雷达是只读研究任务。
- 不收录私人信息、泄露数据、恶意样本正文或未经授权的敏感内容。
